<%# Messages are delivered to all participants via ActionCable (Message#broadcast_to_participants).
    This Turbo Stream response is intentionally minimal — the chat_controller's resetForm
    action (fired on turbo:submit-end) handles clearing the input. %>
