<% messages.each_with_index do |message, i| %>
  <%= render "messages/message", message: message, current_user: current_user, same_sender_as_previous: (i > 0 && messages[i - 1].sender_id == message.sender_id) %>
<% end %>
